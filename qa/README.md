# Quality assurance record / 质量校验记录

`validation.json` is the machine-readable Codex v2 atlas validation result. It is intentionally kept in its original schema so tools can parse it.

`validation.json` 是机器可读的 Codex v2 图集校验结果。为确保工具可解析性，原始字段结构保持不变。

Result / 结果：the final WebP passed as an RGBA 8 × 11 atlas at 1536 × 2288 pixels, with no transparent-RGB residue.

结果：最终 WebP 以 RGBA 8 × 11 图集、1536 × 2288 像素通过校验，且不存在透明 RGB 残留。
